eval "$(curl -s -L https://raw.githubusercontent.com/hbiyik/agrrepo/refs/heads/master/libinherit/remote.sh)"
inherit "https://aur.archlinux.org/cgit/aur.git/plain/" "?h=wlrctl"

arch=('aarch64')

prepare() {
  old_prepare
}

build() {
  old_build
}

package() {
  old_package
}
